# MusicEnhance 开发约定

本文件适用于整个仓库的功能开发、问题修复、重构和文档维护。用户当前任务中的明确要求优先；修改时保留已有功能和用户未提交的改动。

## 项目定位

- 项目名称为「音乐外屏增强」，面向 Xiaomi MIX Flip / MIX Flip 2，使用现代 libxposed API 102。
- 增强播放器在宿主音乐应用进程内运行，由统一的 `EnhancedPlayerActivity` 承载。模块主程序负责配置，不承担音乐播放。
- 当前适配目录为 `qq`、`apple`、`kuwo`、`kugoulite`、`kugou`、`salt`。酷狗普通版与酷狗概念版独立适配，不混用包名、开关和私有接口。
- 应用名单以 `MusicAppRegistry`、`MusicAppAdapters` 和静态作用域为准；版本与 SDK 以 `app/build.gradle.kts` 为准，适配版本见 README。

## 开始修改前

1. 查看工作区状态，阅读相关实现、调用点和已有测试，明确问题所在层级。
2. 优先使用 `rg` 搜索源码。日志、APK 和反编译材料按需读取，避免在整个构建目录中无差别搜索。
3. 先确定根因和受影响路径，再修改。区分宿主私有接口、公共控制器、View 生命周期、系统窗口策略等问题。
4. 修改范围围绕本次目标展开；不要顺手升级依赖、改版本、批量格式化或回退无关文件。
5. 用户取消适配时，清理对应实现、注册、开关、作用域、Manifest 查询、测试和文档；保留仍服务其他应用的通用修复。

## 架构与职责

以下源码位置均相对于 `app/src/main/java/com/jaco/musicenhance`。

| 位置 / 组件 | 职责 | 边界 |
| --- | --- | --- |
| `adapter/<应用>` | 宿主页面识别、反射协议、数据读取和原生操作 | 私有类名、字段及接口只在适配层使用 |
| `MusicPlayerAdapter.createSession()` | 组装 `PlayerSession`、提供器及操作回调 | 不创建各应用专属的增强 UI |
| `PlayerDataSource` | 歌曲快照、控制状态、原生封面和低频样本 | 面向控制器的读取应快速返回，耗时获取由后台任务负责 |
| `LyricsProvider` / `ArtworkProvider` | 歌词、封面及其加载状态 | 由对应适配器创建、管理和释放 |
| `PlayerActions` | 播放、定位、循环、喜欢等 lambda 回调 | 使用明确的操作语义，保留宿主限制 |
| `PlayerController` / `PlayerDisplayState` | 汇总并校验数据，向 UI 提供显示状态 | 显示状态通知在主线程交付 |
| `player/ui` | 展示、动画、触摸交互 | 不读取宿主对象，不选择封面来源，不轮询数据 |
| `player/artwork`、`player/lyrics` | 可复用的数据处理、缓存和交互逻辑 | 不按应用包名分支调用私有接口 |
| `hook` | 模块入口、Activity 路由及系统策略 | 限定目标应用、窗口和生命周期 |
| `device` | 外屏检测、摄像头区域和方向计算 | 设备差异不散落到各播放器适配器 |

- 界面订阅 `PlayerController` 提供的 `PlayerDisplayState`，交互通过控制器转发。不要向 View 增加歌曲匹配、网络请求、歌词解析或封面兜底逻辑。
- 封面与队列提供器由各适配器承载，可复用公共实现；缓存归属对应宿主，不能跨应用混用缓存或原生对象。
- 新能力优先通过现有接口、可选能力状态或注入回调表达，不为单个条件建立新的框架。
- 缺失能力返回未知或不支持，不伪造歌曲、封面、歌词、循环或喜欢状态。现有椒盐喜欢图标固定显示属于用户指定的展示约定，不代表真实收藏，不扩散至其他应用。

## 结构、命名与清理

- 入口统一命名为 `<应用>PlayerAdapter`、`<应用>PlayerProfile`。
- 相同职责沿用 `PlayerHooks`、`ControlSource`、`ArtworkSource`、`ArtworkAddresses`、`LyricsProvider`、`Api` 等既有后缀；不同应用不要为同一职责另造命名体系。
- 类、函数和变量名称描述用途。时间、字节、距离等值标注单位，例如 `positionMs`、`maxBytes`；业务常量放在负责该行为的组件中。
- 复杂动画、音频计算和数据解析从主视图中分离；保持函数和类的职责清楚，避免过度抽象和无意义的转发层。
- 清理本次涉及的无用代码、重复逻辑、失效注释和未使用依赖。不保留废弃实现、演示歌曲或占位歌词。
- 注释解释设计原因、线程要求和兼容约束，不重复代码字面意思。私有接口的版本依据应能追溯。

## 宿主 Hook 与页面生命周期

- 私有 Hook 先校验类、方法签名和必要字段；限制到实际目标页面或对象。接口失效时提供受控降级，避免影响宿主正常启动。
- 复用现有异常隔离与日志机制。异常处理应说明哪个能力失败，不能吞掉错误后返回伪造成功。
- 原生页面提供数据和操作上下文；启动去重、独立 Activity、启动失败清理及小组件显隐走通用路由。
- 冷启动快速点击、页面恢复、内屏播放后折叠到外屏，都可能绕过常规入口，新增路由须考虑这些路径。
- 返回按钮、预测性返回与旧版 `onBackPressed()` 必须共用关闭流程。宿主可能关闭新版返回回调，不能仅凭注册调用成功就认为回调已生效。
- 正常退出先关闭或收起原生播放器，再结束增强 Activity；不能把原生页面退出仅放在 `onDestroy()`。关闭失败应保留可重试状态，手势取消不能提前关闭页面。
- Activity 重建、进程会话丢失和启动失败要分别处理。不得重新使用失效的 Activity、View 或反射对象；失败降级应恢复宿主可操作界面。
- 常亮、系统栏、全屏与方向策略仅作用于模块实际拥有的外屏页面，退出或失去适用条件时释放，保留宿主原有设置。
- 系统窗口策略应区分主应用窗口与 Dialog、Toast 等附属窗口，不能因附属窗口没有模块标记而释放主窗口的全屏状态。
- 摄像头位置与方向使用统一设备坐标依据；考虑反向首页进入播放器及内外屏切换，不仅测试在增强页内旋转。

## 数据、性能与交互

### 异步与资源管理

- 网络、图片解码、数据库访问、原生 IPC 和队列预取不得进入渲染帧或阻塞主线程。
- 宿主对象要求特定线程访问时，在所属线程读取必要字段并复制不可变快照，再交给后台处理。
- 异步结果同时考虑歌曲身份和请求代次；快速切歌、重复操作、取消及释放后，旧结果不得覆盖当前状态。
- 动画、轮询、监听器、任务和提供器必须有对应取消或释放路径。清理应可重复调用，避免全局对象长期持有 Activity。
- 缓存与任务数量有界，复用已有容量限制和队列窗口，不因增加适配器重复保留多份大图。

### 封面与歌词

- 原生模糊背景、默认占位图不能当作高清专辑封面。兜底策略由提供器或控制器处理，下载失败不要无条件清空同曲可用封面。
- 下一首无图、快速前后切歌和旧图延迟返回都要校验，不能通过无限保留上一首封面掩盖缺图。
- 队列预取默认前后各三首，去重并优先最近邻；列表循环和随机模式按宿主实际顺序处理，不能宣称未知随机下一首已缓存。
- 歌词定位使用 `seekAndPlay`，拖动进度使用 `seekTo`，不得用切换播放/暂停代替明确恢复播放。
- 点击歌词要覆盖播放中与暂停状态；宿主需要异步准备时按实际接口顺序处理，并使快速连点只保留最新有效请求。
- 手动滚动接管后，自动歌词动画不能抢占位置。模糊、位移和高亮应协调更新，给回弹和模糊绘制预留边界。
- 频谱在后台处理音频，UI 仅读取已计算样本；不要在每帧调用反射、IPC、解码或频谱分析。

## 新播放器接入清单

1. 核对实际 APK 的包名、版本、播放入口、页面形态和所需私有接口。
2. 添加 Profile、Adapter，并接入现有注册机制与 `createSession()`。
3. 实现入口触发和原生页面退出，复用 `EnhancedPlayerActivity` 与公共窗口策略。
4. 补齐歌曲状态、播放控制、歌词、封面、循环及喜欢能力；记录不支持项。本地播放器优先复用其原生数据与缓存。
5. 更新设置开关、静态作用域和 Manifest 查询配置；作用域变更需安装新包，系统侧名单更新需重启加载。
6. 按实际风险验证冷启动、首次全屏、切歌、返回、旋转和内外屏切换，更新 README 的适配版本及限制。

仅新增包名、编译通过或成功打开页面，不等于完成播放器适配。

## 验证与发布

### 验证方式

- 行为修改优先增加或运行能复现真实风险的回归测试，例如切歌竞态、返回顺序、手势取消、快速操作、缺失数据和生命周期。
- 返回问题须覆盖宿主实际使用的事件入口，不能只直接调用内部回调。窗口和旋转问题须结合设备状态及真机画面确认。
- 纯文档或格式调整检查内容、链接和差异即可，不为此新增机械测试或重复构建。
- 不为通过检查而删除有效测试、扩大忽略范围或批量压制警告。必要的兼容性抑制限定到最小范围，并说明依据。
- 尊重现有测试目录和忽略规则；确认当前检出是否真的包含测试。`NO-SOURCE`、跳过或旧报告不能报告为本次测试通过。

发布包前在项目根目录运行（PowerShell）：

```powershell
.\gradlew.bat --no-configuration-cache :app:testDebugUnitTest :app:lintRelease :app:assembleRelease
```

macOS / Linux 使用 `./gradlew`。调整发布脚本时还需运行：

```text
python -m unittest discover -s .github/scripts/tests -v
```

- 检查实际执行结果、失败数量和剩余警告，不只看任务退出或 APK 文件存在。
- 真机测试前确认连接的设备和前台应用；用户正在操作或切换应用时先协调，避免测试点击落入无关页面。
- 应用侧 Hook 更新需强行停止并重开宿主；系统框架或 System UI Hook 更新需重启才能加载。安装完成不代表新 Hook 已运行。
- 自动化检查不能替代外屏布局、系统窗口、手势和宿主私有接口的真机验证；ADB 不可用时明确标记待验证项。

### 版本、签名与交付

- 版本以现有构建文件为准；用户要求修改版本时，同时递增 `versionCode`，保持 `versionName` 与发布规则一致。
- 自动发布规则见 `.github/workflows/release.yml` 和 `.github/scripts/release.py`：数字版本为正式版，`-beta` 后缀为预发布版；标签须匹配版本，已公开版本不覆盖。
- 签名使用本机环境变量或 Actions Secrets。证书、密码、个人路径、设备日志、反编译产物和构建包不得写入受版本控制的源码或文档。
- 提供签名包时，核验签名证书及 APK 中的实际版本；安装后另行确认设备安装结果，不把构建成功当作安装成功。
- 功能、适配范围或开发流程变化同步 README；保留用户已有文档改动，避免加入未经验证的兼容性承诺。
- 交付说明包括改动内容、验证结果、剩余限制和是否已安装。没有实际执行的提交、推送、发布及真机测试不得声称完成。
