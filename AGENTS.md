# 项目开发约定

后续新增功能、修复和重构均遵循以下要求。

## 结构与可读性

- 修改前阅读相关实现和调用点，保留用户已有改动；以明确的问题和职责为边界进行整理。
- 清理本次改动涉及的无用代码、重复逻辑、失效注释和未使用依赖，不保留废弃实现或占位演示数据。
- 使用能说明用途的类、函数和变量名称。时间、距离等数值标明单位；将有业务含义的常量集中到负责该行为的组件。
- 视图负责展示和交互，控制器负责状态与操作，数据提供器负责获取数据。复杂动画和计算应独立于播放器主视图。
- 避免为单个简单条件引入框架或过度抽象；注释解释原因、边界和兼容约束。
- 播放器适配入口统一使用 `<应用>PlayerAdapter`、`<应用>PlayerProfile`，通过 `createSession()` 组装数据提供器和 `PlayerActions` 回调。同一职责沿用 `PlayerHooks`、`ControlSource`、`ArtworkSource`、`ArtworkAddresses`、`LyricsProvider` 等现有后缀；不要为相同职责另造 Callback/Manager 等名字。

## 多播放器兼容

- 通用 UI、歌词、封面缓存和队列逻辑通过 `PlayerController`、`LyricsProvider`、`ArtworkProvider` 等接口访问能力，不直接依赖 QQ 类名、反射字段或网络接口。
- 界面通过 `PlayerController` 订阅整理好的 `PlayerDisplayState`，只负责展示、动画和用户交互；不要在视图中增加封面来源选择、数据轮询、歌词加载或歌曲匹配逻辑。各适配器创建并管理数据提供器，可复用通用缓存实现，缓存保存在对应宿主应用内。
- 应用私有逻辑集中在 `adapter/<应用>`，应用识别和控制器创建走现有注册机制；设备和摄像头差异归入 `device`。
- 增强界面统一由 `EnhancedPlayerActivity` 承载；原生页面只提供数据与操作上下文。启动、退出和小组件显隐走通用路由及窗口标记，各应用只实现触发/原生页面退出差异。
- 新能力优先通过可选接口、能力状态或注入的回调接入；缺失能力返回不支持或未知，不能伪造喜欢状态、循环方式、封面或歌词。
- 新播放器需补齐 Profile、工厂、原生接口、作用域和查询配置，并验证实际版本；只增加包名不代表完成兼容。

## 性能与生命周期

- 网络、图片解码、队列预取等耗时工作不得放到渲染帧或主线程中。复用现有缓存，并限制缓存容量和预取范围。
- 异步结果校验歌曲标识或请求代次，防止切歌后旧结果覆盖新数据。
- 动画、定时任务、监听器和控制器必须有对应的取消或释放路径；触摸接管后自动滚动不得覆盖手动滑动。
- 常亮、系统栏和方向策略只在模块实际拥有的外屏页面生效，退出后释放，保留宿主原有设置。
- 留意 Android View 构造和回调顺序，供初始化或父类回调使用的属性应在使用前就绪。

## 验证与交付

- 对行为变化运行相关回归测试；优先覆盖切歌竞态、快速切换、手动操作、缺失数据和生命周期等真实风险，不为简单格式修改添加机械测试。
- 发布前运行单元测试、Release 静态检查和构建；如提供签名包，核验签名和 APK 实际版本。
- 修改版本时同时递增 `versionCode`，保持 `versionName` 与发布规则一致。
- 说明已验证范围与尚未完成的真机验证；自动化检查不能替代外屏旋转、系统窗口和音乐应用私有接口验证。
- 功能或开发流程变化同步 README。证书密码、个人路径、日志、构建产物不得写入受版本控制的文档或源码。
